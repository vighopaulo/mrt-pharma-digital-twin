# MRT Pharma Digital Twin V6 — Sprint 2.1 Build 1

This build introduces the first spatial domain primitives:

- `Coordinates3D`
- `Orientation3D`
- Unit tests for distance, translation, normalization, rotation, and validation

## Run tests

```bash
python -m pip install -e ".[test]"
pytest
```
